import React from 'react';

import { Container } from 'pages/detail/detail-content';

const Detail = ({ location }) => {
  return (
    <Container location={location} />
  )
}

export default Detail;
